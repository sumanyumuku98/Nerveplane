node_modules/
/dist/
docs/.vitepress/dist/
docs/.vitepress/cache/
.DS_Store
*.log

# dashboard/dist/index.html is committed (single-file build embedded by the daemon)
# so the static `with { type: "text" }` import always resolves.

# Local nerveplane state (when run from within this repo during dev)
.nerveplane/

# Local agent/worktree tooling — not part of the project
.claude/
.dmux/
.dmux-hooks/
